<%= form_with(model: insea_food, local: true) do |form| %>
  <% if insea_food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(insea_food.errors.count, "error") %> prohibited this insea_food from being saved:</h2>

      <ul>
      <% insea_food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :information %>
    <%= form.text_area :information %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
