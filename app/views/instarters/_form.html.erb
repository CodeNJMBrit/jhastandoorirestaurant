<%= form_with(model: instarter, local: true) do |form| %>
  <% if instarter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(instarter.errors.count, "error") %> prohibited this instarter from being saved:</h2>

      <ul>
      <% instarter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :information %>
    <%= form.text_area :information %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
