<p id="notice"><%= notice %></p>

<h1>Vegetable Dishes</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Information</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @invegetable_dishes.each do |invegetable_dish| %>
      <tr>
        <td><%= invegetable_dish.name %></td>
        <td><%= invegetable_dish.information %></td>
        <td>£<%= invegetable_dish.price %></td>
        <td><%= link_to 'Show', invegetable_dish %></td>
        <td><%= link_to 'Edit', edit_invegetable_dish_path(invegetable_dish) %></td>
        <td><%= link_to 'Destroy', invegetable_dish, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Vegetable Dish', new_invegetable_dish_path %>
