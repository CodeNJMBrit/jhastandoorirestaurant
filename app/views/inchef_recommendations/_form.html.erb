<%= form_with(model: inchef_recommendation, local: true) do |form| %>
  <% if inchef_recommendation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inchef_recommendation.errors.count, "error") %> prohibited this inchef_recommendation from being saved:</h2>

      <ul>
      <% inchef_recommendation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :information %>
    <%= form.text_area :information %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit 'Save Dish' %>
  </div>
<% end %>
