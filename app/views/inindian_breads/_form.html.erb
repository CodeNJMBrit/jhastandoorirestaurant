<%= form_with(model: inindian_bread, local: true) do |form| %>
  <% if inindian_bread.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inindian_bread.errors.count, "error") %> prohibited this inindian_bread from being saved:</h2>

      <ul>
      <% inindian_bread.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :information %>
    <%= form.text_area :information %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
