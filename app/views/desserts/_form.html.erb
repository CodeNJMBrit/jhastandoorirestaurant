<%= form_with(model: dessert, local: true) do |form| %>
  <% if dessert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dessert.errors.count, "error") %> prohibited this dessert from being saved:</h2>

      <ul>
      <% dessert.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :information %>
    <%= form.text_area :information %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit 'Save Dish' %>
  </div>
<% end %>
